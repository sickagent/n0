# Binaries
/bin
/dist
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html

# Go workspace checksum (generated)
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Env files
.env
.env.local

# Terraform
*.tfstate
*.tfstate.*
.terraform/
terraform.tfvars

# Protobuf Go code is committed because it is a referenced workspace module.
# CI regenerates it and verifies that contracts remain in sync.
